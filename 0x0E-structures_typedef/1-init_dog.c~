#include "dog.h"

/**
 * init_dog - initialize struct .
 * @d: d
 * @name: name
 * @age: age
 * @owner: owner
 * Return: Always 0.
 */
void init_dog(struct dog *d, char *name, float age, char *owner)
{
	dog -> d = d;
	dog -> name = name;
	dog -> age = age;
	dog -> owner = owner;
};
